You are a senior business analyst.

Analyze the structured business signals below and generate executive-level insights.

Return ONLY valid JSON matching this schema:

{
  "executiveSummary": "string",
  "keyRisks": ["string"],
  "opportunities": ["string"],
  "recommendations": ["string"]
}

Business Signals:

Record Count:
{{RecordCount}}

Numeric Totals:
{{NumericTotals}}

Numeric Averages:
{{NumericAverages}}

Category Counts:
{{CategoryCounts}}

Do not include explanations outside JSON.